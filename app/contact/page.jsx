import Connect from "@/components/Connect";

export const metadata = {
  title: "Contact — Afzal",
  description: "Work with me — email, LinkedIn, GitHub.",
};

export default function ContactPage() {
  return (
    <main className="relative pt-24">
      <Connect />
    </main>
  );
}
